'use client';

export function HeroSubheadline() {
  return (
    <p className="max-w-2xl text-lg leading-relaxed text-foreground">
      Generate a client-side SHA-256 hash. Anchor it to the Stellar network using Soroban. No middlemen, no centralized servers.
    </p>
  );
}
